// =============================================================================
// postcss.config.js — PostCSS Configuration สำหรับ Tailwind CSS v3
// ใช้งานร่วมกับ: tailwind.config.js, src/app/globals.css
// =============================================================================

module.exports = {
  plugins: {
    tailwindcss: {},   // Tailwind CSS v3
    autoprefixer: {},  // เพิ่ม vendor prefixes อัตโนมัติ
  },
}
